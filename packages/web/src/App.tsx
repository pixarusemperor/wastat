import { WASTAT_VERSION } from "@wastat/shared";

export default function App() {
  return (
    <main>
      <h1>Wasender Workflow Engine</h1>
      <p>Shared package version: {WASTAT_VERSION}</p>
    </main>
  );
}
