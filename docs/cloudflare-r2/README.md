# cloudflare-r2 documentation snapshot

- **Source URL:** https://developers.cloudflare.com/r2/
- **Downloaded:** 2026-08-21
- **SHA-256:** see `manifest.json` per file
- **Files:** 79 (741,543 bytes)

This snapshot is a local, versioned copy of the Cloudflare R2 — S3-compatible object storage, presigned URLs, uploads/downloads, security. It must NOT be fetched at runtime (PRD §40). Refresh deliberately when upstream docs change.

## Structure

Raw `llms.txt` index preserved; each page split into `<section>.md`.

| Section | Pages |
|---|---|
| `(root)` | 11 |