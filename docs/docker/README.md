# docker documentation snapshot

- **Source URL:** https://docs.docker.com
- **Downloaded:** 2026-08-21
- **SHA-256:** see `manifest.json` per file
- **Files:** 2 (329,512 bytes)

This snapshot is a local, versioned copy of the Docker — images, containers, volumes, compose (Coolify deployment target). It must NOT be fetched at runtime (PRD §40). Refresh deliberately when upstream docs change.

## Structure

- `llms.txt` — page index with descriptions
- `llms-full.txt` — full text corpus of all doc pages (grep this first)
