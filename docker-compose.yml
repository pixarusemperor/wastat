# Local run of the production image (Coolify builds the same Dockerfile).
services:
  server:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
